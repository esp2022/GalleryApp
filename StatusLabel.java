package cs1302.gallery;

import javafx.scene.control.Label;

/** Establishes status label. */
public class StatusLabel extends Label {

/** Constructor. */
    StatusLabel() {
        setText("Type in a term, select a media type, then click the button.");
    }
}
